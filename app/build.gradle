apply plugin: 'com.android.application'

android {
    compileSdkVersion 21
    buildToolsVersion "20.0.0"

    defaultConfig {
        applicationId "sergio1tsov.english_quiz.com.englishquiz"
        minSdkVersion 11
        targetSdkVersion 21
        versionCode 4
        versionName "2.0.0"
    }

}

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
}
